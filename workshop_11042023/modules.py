#allias
import myMath as mm

from myMath import divide #also can use with as no problem with more import
#built in modolues
import random as random

from selenium import webdriver

print(mm.sum(3,5))
print(divide(8,4))

print(random.randint(0,100))

#chromeDriver=webdriver.Chrome()